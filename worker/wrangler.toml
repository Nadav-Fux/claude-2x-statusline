name = "statusline-telemetry"
main = "worker.js"
compatibility_date = "2024-01-01"
# Self-hosters: replace with your own Cloudflare account id (`wrangler whoami`).
account_id = "REPLACE_ME"

[[kv_namespaces]]
binding = "TELEMETRY"
# Self-hosters: replace with your own KV namespace id
# (`wrangler kv:namespace create TELEMETRY`).
id = "REPLACE_ME"
